@model CaligulasHotel.Models.ViewModel.TipoVehiculoViewModel

@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    ViewBag.Title = "Eliminar Tipo Vehíco";
}

<h2 class="display2 text-light text-upper">@ViewBag.Title</h2>

<div class="row">
    <div class="cell-12 px-0">
        <ul class="breadcrumbs">
            <li class="page-item"><a href="@Url.Action("TipoVehiculo","Admin",new { })" class="page-link">TIPO VEHÍCULOS</a></li>
            <li class="page-item"><a href="#" class="page-link">ELIMINAR TIPO VEHÍCULO</a></li>
        </ul>
    </div>
    <div class="cell-md-6 cell-sm-12">
        <div class="panel border bd-red win-shadow" data-role="panel">
            <h3>¿Esta Seguro de Eliminar el Registro?</h3>
            <dl>
                <dt>ROWID</dt>
                <dd>@Html.DisplayFor(x => x.TipoVehiculoId)</dd>
                <dt>NOMBRE</dt>
                <dd>@Html.DisplayFor(x => x.Nombre)</dd>
            </dl>
        </div>
        @using (Html.BeginForm("DeleteTipoVehiculoPost", "Admin", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="form-group">
                @Html.HiddenFor(x => x.TipoVehiculoId)
            </div>
            <div class="form-group">
                <button type="submit" class="button alert"><span class="mif-bin icon"></span> ELIMINAR</button>
            </div>
        }
    </div>
</div>